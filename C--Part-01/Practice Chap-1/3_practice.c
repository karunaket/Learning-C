#include <stdio.h>
#include <conio.h>
void main()
{
  int radius = 5;
  float pi = 3.14;
  printf("The area of circle is %f \n", pi * radius * radius);
  int height = 7;
  printf("Volume of cylinder is %f", pi * radius * radius * height);
  getch();
}